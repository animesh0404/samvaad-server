export const environment = {
  production: false,
  // Same-origin relative API base. In development the ng serve proxy
  // forwards /api to the backend at http://localhost:8080.
  apiBaseUrl: '',
};
