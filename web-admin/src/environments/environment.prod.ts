export const environment = {
  production: true,
  // Production assets are served same-origin with the Samvaad backend
  // (future packaging slice), so no host is baked into the bundle.
  apiBaseUrl: '',
};
