SAMVAAD_JWT_SECRET=replace-with-a-local-development-secret
# Deployment database password shared by PostgreSQL and the application
# through the root compose.yaml. Leave unset here: scripts/install.sh and
# scripts/start.sh generate a secure value automatically when none is
# configured. Never commit a real secret in this file.
# SAMVAAD_DB_PASSWORD=
